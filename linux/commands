journalctl 
netstat
ps 
ps -aux
top
curl ifconfig.me


#!/bin/bash
cat <<EOF > deployment.yml
this is first line
this is second line
EOF

usermod -aG sudo n1

find ./GFG -name sample.txt 
find ./GFG -name *.txt 
find ./GFG -name sample.txt -exec rm -i {} \; 
find ./GFG -empty
find ./GFG -perm 664

ssh -i /home/ubuntu/cicdutiliko.pem ubuntu@43.201.62.110
ssh -i /home/ubuntu/cicdutiliko.pem ubuntu@43.201.62.110 sudo docker logs utilko-api --follow

id prometheus

sudo lsof -i -P -n | grep LISTEN

lsof -i -P -n | grep ':3000'

df -h

iptables -A INPUT -p tcp -m tcp --dport 3001 -m state --state NEW -j ACCEPT

ps aux
ps aux --sort -%cpu
ps aux --sort -%mem
ps aux --sort -%mem | head -10

top -o %MEM

head -n 16
head -n 4 example1.txt

journalctl -xe

sudo find / -type s

cd /etc/init.d

export AWS_ACCESS_KEY_ID=AK************IEVXQ

df

vi

kill

ls

chmod
chmod 600 *

init

find

ping

wget

grep

nslookup

netsat

https://www.geeksforgeeks.org/sed-command-in-linux-unix-with-examples/

sed 's/unix/linux/' file.txt   #replace first occurrence
sed 's/unix/linux/2' file.txt  #replace second occurrence 
sed 's/unix/linux/3' file.txt  #replaces third occurence
sed 's/unix/linux/g' file.txt  #replaces all occurences
sed 's/unix/linux/3g' file.txt  #replaces all occurrences from third 
sed '3 s/unix/linux/' file.txt  #replaces occurrences in third line only.
sed 's/unix/linux/p' file.txt   #replced line prints twice but not replaced lines prints only once.
sed -n 's/unix/linux/p' file.txt #prints only replaced lines 
sed '1,3 s/unix/linux/' file.txt #replaces as specied range , 1,3 is the range of lines

sed 'nd' file.txt # for deleting nth line
sed '5d' file.txt # for deleting 5th line 
sed '$d' file.txt # for deleting last line
sed '3,6d' file.txt # for deleting from range 3 to 6d

sed '/pattern/d' file.txt # for deleting the pattern matching line 

https://www.geeksforgeeks.org/uname-command-in-linux-with-examples/

uname -a  # kernal name, network node hostname, kernal release date, kernal version, machine hardware name, hardware platform, operating system
uname -s # kernal name 
uname -n # prints hostname
uname -r # kernels release date
uname -v # prints kernel version
uname -m # prints machine harware name 
uname -p # prints the type of processor
uname -o # prints the name of operating system

##################################
ps 
ps aux
ps -ef 

kill PID
killall process_name
kill -9 PID 

top
htop

free 

df 
df -h 

du
du -h 

netstat
netstat -tuln  : show all listening tcp and udp ports 

ifconfig

ip address show
ip link
ip route

route -n 

iptables

ufw

useradd
userdel
usermod

password

groupadd
groupdel

su

sudo

crontab 

at 

journalctl

tar

rsync

########################################################
########################################################

find /path/to/directory -name "redis.conf"
telnet 65.2.189.79 6379




